cd "$(git rev-parse --show-toplevel)" && cd "$(git rev-parse --show-toplevel)" || exit 1
cd "$(git rev-parse --show-toplevel)" && cd "$(git rev-parse --show-toplevel)" && cd "$(git rev-parse --show-toplevel)" && cd "$(git rev-parse --show-toplevel)" && cd "$(git rev-parse --show-toplevel)" && cd "$(git rev-parse --show-toplevel)" && npx lint-staged
